let mutation = arr => {
  let str1 = arr[1].toLowerCase()
  let str2 = arr[0].toLowerCase()
  for (let x = 0; x < str1.length; x++) {
    if (str2.indexOf(str1[x]) < 0) return false
  }
  return true
}

mutation(["hello", "hey"])
